# 1. Data Shape 확인하기
```python
spotify_df.shpae  # (114000, 20), 114000개의 데이터와 20개의 컬럼
```
---
# 2. Data Type 확인
```python
spotify_df.info()
```
![image](https://github.com/user-attachments/assets/8d408641-ab31-4cde-96fe-6c2d1a04baff)

---
# 3. 결측값 확인
```python
spotify_df.isna().sum() 
```
![image](https://github.com/user-attachments/assets/aab41e6e-63cf-4942-9917-0752eb38d1ed)

## `artists`, `album_name`, `track_name` 컬럼에서 결측값 1개씩 발견
```python
target_cols = ['artists', 'album_name', 'track_name'] # OR 조건으로 세 컬럼 중 하나라고 결측값을 갖는 로우를 모두 출력
spotify_df[spotify_df[targer_cols].isna().any(axis = 1)]
```
![image](https://github.com/user-attachments/assets/f9e3e35c-27f8-471a-9898-73f3a8e197ee)
##  모두 '1kR4gIb7nGxHPI3D2ifs59'라는 `track_id`를 갖는 하나의 곡에 존재하는 결측값인 걸로 확인

---
# 4. 중복 데이터 확인
```python
spotify_df.duplicated().sum() # 450개의 중복 데이터 확인
```
```python
soptify_df = soptify_df.drop_duplicated() # 4-1 중복 데이터 삭제
```
```python
spotify_df.duplicated(subset ='track_id').sum() # subset을 이용해 track_iddp 23809개의 중복값 확인
```
```python
spotify_df['track_id'].value_counts() # track_id컬럼에서 가장 많이 나타나는 상위 값과 그 빈도수 출력
```
![image](https://github.com/user-attachments/assets/3f73bf2b-0c19-4faa-8403-ec99c5c2920e)
---
```python
spotify[spotify['track_id'] == '6S3JlDAGk3uu3NtZbPnuhS'] #track_id가 6S3JlDAGk3uu3NtZbPnuhS인 로우를 모두 출력
```
![image](https://github.com/user-attachments/assets/ce612533-2e5c-40bf-8d7f-d85d7b5f4340)

## track_genre를 제외한 모든 값이 동일. 즉 하나의 곡에 여러개의 장르를 가지고 있음

```python
target_columns = [col for col in spotify_df.columns if col != 'track_genre'] # track_genre만 제외하고 나머지 컬럼을 리스트로 담기
targer_columns
```
```python
spotify_df.groupby(targer_columns), agg({'track_id' : lambda x: ', '.join(x)}).reset_index() #동일한 곡에 대해, track_genre를 콤마로 구분된 문자열로 묶어줌
```
```python
spotify_df.duplicated(subset = 'track_id').sum() # track_id를 기준으로 다시 조회, 여전히 720개의 중복값 확인
```
```python
spotify_df['track_id'].value_counts()
```
![image](https://github.com/user-attachments/assets/ab815a32-ee5d-41b3-bce0-75391530a8cd)

```python
spotify_df[spotify_df['track_id'] == "0vp47IigZ7JVQ89jDetO7H"]
```
![image](https://github.com/user-attachments/assets/a2dc127d-e701-49b3-bedd-750ca5a5f07f)
## track_genre 외에도 popularity에 근소한 차이가 있어 중복 제거가 되지 않은 경우로 보임

```python
spotify_df.columns.drop(['popularity', 'track_genre']).tolist() # 두 컬럼을 동시에 제외한 후 리스트로 변환
```

```python
spotify_df.groupby(target_columns).agg({'popularity': 'mean', 'track_genre' : lambda x: ', '.join(x)}).reset_index() # 동일한 곡에 대해, track_genre는 콤마로 구분된 문자열로, popularity는 평균값으로 묶어줌

spotify_df.duplicated(subset='track_id').sum() # 중복값이 다 사라졌는지 확인

spotify_df.shape # 89740, 20 데이터가 줄어든거를 확인할 수 있음
```















