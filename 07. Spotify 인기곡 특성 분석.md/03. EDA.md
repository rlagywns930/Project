# 1. 인기도 점수 영향 요인 분석
```python
numeric_list = spotify_df.select_dtypes(include = 'number').columns.to_list() # 숫자형 변수를 리스트로 만들기
categorical_list = spotify_df.select_dtypes(include = ['str', 'bool']).columns.to_list() # 범주형 변수를 리스트로 만들기
```
---
# 1-1 인기도 점수 분포 확인
```python
#시각화에 필요한 라이브러리 불러오기
import matplotlib.pyplot as plt
import seaborn as sns

plt.figure(figsize = (20,5))

sns.histplot(data = spotify, x = 'popularity', kde = True, stat = 'density', palette = 'Blues')
```
![image](https://github.com/user-attachments/assets/9d93cc53-6643-4db1-a3ac-e053a6293bfb)

## 0점 부근에 많은 곡들이 포진해 있음 10~60점 까지는 고르게 분포해 있음

---
# 1-2 인기도 점수와 기타 숫자형 변수 간 상관관계
```python
plt.figure(figsize=(12, 6))

corr_matrix = spotify_df[numeric_list].corr()[['popularity']].drop('popularity', axis=0)
sns.heatmap(corr_matrix, annot=True, fmt='.2f', cmap='vlag', vmax=1, vmin=-1)

plt.show()
```
![image](https://github.com/user-attachments/assets/89ca99a4-d0ad-4d93-be93-e0db92a72619)

## popularity와 상관관계가 큰 변수는 없다 그나마 높은 변수는 instrumentalness
---
## popularity와 instrumetalness 사이의 관계를 파악하기
```python
sns.scatterplot(data = spotify_df, x= 'instrumetalness', y = 'popularity')
plt.show()
```
![image](https://github.com/user-attachments/assets/26d5bc2c-7692-4250-b2fd-7e9e61c5e065)

## popularity와 instrumetalness 큰 관련성이 없어 보임

---
# 1-3 explicit 표현 포함 여부에 따른 인기도 차이

```python
fig, (ax1,ax2) = plt.subplots(1,2, figsize = (15,5))

sns.boxplot(data = spotify_df, x = 'explicit', y = 'popularity', hue = 'explicit', palette = 'Blues', legend = False, ax = ax1)

sns.barplot(data = spotify_df, x = 'explicit', y = 'popularity', hue = 'explicit', palette = 'Blues', legend = False, ax = ax2)

for c in ax2.containers:
    ax.bar_lable(c, fmt = '{:.1f}', label_types = 'center')

plt.show()
```
![image](https://github.com/user-attachments/assets/6d7bde6e-1990-4e77-99b1-efa31d5714ef)








